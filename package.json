{
  "name": "godmatsmak",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "dev": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
